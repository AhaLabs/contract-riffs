[package]
edition = "2021"
name = "near-components-admins"
version = "0.1.0"
publish = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
near-components = {path = "../.."}
near-components-core = {path = "../core"}
microjson = "0.1.0"

[features]
wee_alloc = ["near-components/wee_alloc"]
